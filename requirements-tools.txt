# Optional dependencies for GAIA file parsing and MARBLE DB.
-r requirements.txt
openpyxl==3.1.5
pandas==2.3.3
Pillow==11.3.0
psycopg2-binary==2.9.11
python-docx==1.2.0
python-pptx==1.0.2
pyarrow==23.0.0

# Install only when needed and compatible with your CUDA stack:
# bitsandbytes==0.50.0   # --load-in-4bit
# vllm==0.14.0          # text/single-agent generation only
